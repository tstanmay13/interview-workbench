# Discovery wave

Propose at most two non-overlapping read-only investigations. Spawn only tasks that pass
all five tests in the operating brief. Keep the central execution path in the main thread.

For each task, provide: objective, why it matters, allowed repository/path scope, what not
to do, time/stopping condition, required evidence, and the evidence-packet return format.
While workers run, continue baseline reproduction or direct inspection here.
