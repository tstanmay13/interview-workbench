# Review console

A dependency-free browser surface for exercising the case service.

## Run

```sh
npm start
```

The server listens on `PORT` (default `4200`). By default the browser calls the
case service at `http://127.0.0.1:4100`.
